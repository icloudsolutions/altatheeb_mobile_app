# Leave empty when the admin SPA is served from the same host as the API (nginx in deploy/ems).
# Local dev: use Vite proxy (empty) or set explicitly, e.g.:
# VITE_API_BASE_URL=http://localhost:8000
VITE_API_BASE_URL=
